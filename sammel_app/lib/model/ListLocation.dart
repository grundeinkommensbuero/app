class ListLocation {
  int id;
  String name;
  String street;
  String number;
  double latitude;
  double longitude;

  ListLocation(this.id, this.name, this.street, this.number, this.latitude,
      this.longitude);
}
